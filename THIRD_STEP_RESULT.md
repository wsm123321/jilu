# 第三步结果：统计可辨识性、选择性风险与模型失配

## 1. 总结

Gate 3 已完成三部分实验：

- A：精确二次模型下的噪声压力与选择性门控；
- B：无噪声四次项模型失配与区域尺度效应；
- C：有限代表性组合压力测试。

核心结论是：

> 满秩只说明模型可以求解，不说明属性安全；标准化设计条件数能描述几何风险，但单独不能预测属性是否可靠；不确定性门控在精确二次模型的总体保留集上有效，却不能保证每个噪声层都安全，更无法识别非二次模型带来的结构偏差。

因此 Gate 3 没有给出“可以开始迁移”的统一绿灯，而是识别出两类必须分开处理的失败：

1. **统计方差失败**：噪声、小样本和轨迹聚集造成，可由不确定性证据部分检测；
2. **估计对象偏移**：非二次性、区域尺度和采样测度造成，普通残差不确定性无法检测。

## 2. 正交景观与配对设计

使用

\[
K(q,r,\theta)=q\sqrt2Q_\theta\frac{\operatorname{diag}(1,r)}{\sqrt{1+r^2}}Q_\theta^\top,
\]

使 \(q=\|K\|_F/\sqrt2\)、\(r=\kappa(K)\)。噪声为 \(\sigma=\eta q\)。同一采样器、样本量和 seed 在所有景观条件下共享点和标准正态噪声前缀。

审计显示，在固定 \(r\) 下，\(q=0.5,1,2\) 的相对误差和失败率逐项一致，说明旧设计中“曲率量级与信号幅度混杂”的问题已被消除。

## 3. 第三步 A：噪声压力

### 3.1 数据规模

- 开发集：50 seeds，129,600 条；
- 保留集：100 个全新 seeds，259,200 条；
- 正式仓库中的长表以确定性 `.csv.gz` 发布，解压后即为完整 CSV；汇总表和图片保持未压缩，并由 bundle manifest 记录 SHA-256；
- 样本量：6、8、12、20；
- 噪声比例：0、0.01、0.05、0.10；
- 采样器：Sobol prefix、Uniform、Trajectory；
- 中心状态：驻点与非驻点；
- 可靠事件：曲率量级误差≤0.2、带符号谱形误差≤0.15、估计 Hessian 为 SPD。

### 3.2 开发集冻结阈值

| Gate | 冻结阈值 | 开发覆盖率 | 开发风险 |
|---|---:|---:|---:|
| Condition | 无安全阈值 | 0 | 未定义 |
| Oracle uncertainty | 相对量级 SE ≤ 0.10 | 53.4% | 5.4% |
| Residual uncertainty | 相对量级 SE ≤ 0.10 | 47.4% | 7.9% |

Condition 候选阈值网格中没有任何阈值能把开发集 selective risk 压到 10% 以下，因此冻结为“无安全阈值”，而不是事后放宽目标。

### 3.3 保留集总体风险—覆盖率

| Gate | 覆盖率 | Selective risk | 接受且失败的总体比例 |
|---|---:|---:|---:|
| Rank-Only | 99.85% | 35.61% | 35.56% |
| Condition | 0% | 未定义 | 0% |
| Oracle uncertainty | 54.61% | 4.90% | 2.68% |
| Residual uncertainty | 48.63% | 8.56% | 4.16% |
| Combined | 0% | 未定义 | 0% |

总体上，两个 uncertainty gate 在全新保留种子上维持了开发阶段的目标风险，而 Rank-Only 明显不安全。

### 3.4 总体平均不能代表所有噪声层

Oracle uncertainty 在 \(\eta=0.05\) 时风险为 13.50%，在 \(\eta=0.10\) 时为 33.46%。Residual uncertainty 更明显：

| 条件 | 覆盖率 | 风险 |
|---|---:|---:|
| n=8 | 57.13% | 12.95% |
| n=12 | 64.44% | 7.64% |
| n=20 | 72.96% | 5.92% |
| eta=0.01 | 69.71% | 4.95% |
| eta=0.05 | 38.10% | 19.64% |
| eta=0.10 | 11.72% | 48.76% |
| Sobol prefix | 57.06% | 7.23% |
| Uniform | 51.63% | 8.80% |
| Trajectory | 37.20% | 10.26% |

因此，单一总体阈值只能提供总体经验保证，不能宣称对高噪声或轨迹采样条件安全。\(n=6\) 的残差自由度为 0，Residual uncertainty 正确地全部拒绝。

### 3.5 属性分类

- 无噪声：完整二次属性在满秩时可恢复，但这只是 Gate 2 已知结论；
- \(\eta=0.01\)：在 uncertainty gate 下可视为 Oracle 表示层的候选；
- \(\eta\ge0.05\)：当前统一门控不能提供逐层≤10%风险保证；
- n=8：Residual uncertainty 仍不稳定；
- n≥12、低噪声：可考虑 `Mid-stage candidate`；
- trajectory：应标记为 `Sampler-dependent risk`。

当前证据不支持把曲率量级或谱宣布为通用 `Early-stage candidate`。

## 4. 符号与排序指标修正

Gate 3 新增最小特征值、SPD、inertia mismatch 和负特征值比例。带符号谱形误差用于准入，旧的绝对特征值谱只保留为兼容诊断，不能掩盖盆地—鞍点类型错误。

固定探针排序被降级为 Hessian 派生诊断，并同时报告 pair coverage 与 selective accuracy。它不构成第三个独立可辨识属性。

## 5. 第三步 B：非二次模型失配

景观为

\[
g(u)=c+b^\top u+\frac12u^\top K_0u+\beta(u_1^4+u_2^4).
\]

区域尺度 \(\rho\in\{0.25,0.5,1\}\)，\(\beta/q\in\{0,0.05,0.20,0.50\}\)。共 64,800 条。

静态采样器的投影目标对应 \([ -\rho,\rho]^2\) 上均匀面积测度，由 8×8 Gauss–Legendre 加权求积定义；trajectory 的目标绑定 500 条冻结生成机制路径的 pooled empirical measure。

### 5.1 投影偏差随区域尺度快速增长

均匀面积测度下：

| rho | beta/q=0.05 | beta/q=0.20 | beta/q=0.50 |
|---:|---:|---:|---:|
| 0.25 | 0.005 | 0.021 | 0.054 |
| 0.50 | 0.021 | 0.086 | 0.214 |
| 1.00 | 0.086 | 0.343 | 0.857 |

trajectory 经验测度下，在 \(\rho=1,\beta/q=0.5\) 时投影偏差中位约 0.996，高于均匀面积测度的 0.857。

这说明在非二次景观中，完整二次回归即使样本无限多，也可能收敛到依赖区域尺度和采样测度的投影，而不是中心 Hessian。必须保存 `属性 + 区域尺度 + 采样定义`，不能只保存一个无上下文 Hessian。

## 6. 第三步 C：组合压力

使用 A 冻结的 Residual uncertainty 阈值，不重新调参，共 18,000 条。

| 场景 | 覆盖率 | 接受后风险 | 未门控失败率 |
|---|---:|---:|---:|
| 低噪声 + 弱非二次 | 57.22% | 15.99% | 35.78% |
| 低噪声 + 强非二次 | 82.59% | 99.93% | 99.89% |
| 高噪声 + 弱非二次 | 0.59% | 87.50% | 97.26% |
| trajectory + 非驻点 | 0.89% | 93.75% | 96.72% |

最关键的反例是“低噪声 + 强非二次”：残差门控高覆盖地接受了几乎全部错误属性。原因是二次模型可以稳定估计其区域投影，残差/方差并不知道研究目标是中心 Hessian。这不是门控阈值略差，而是**估计对象错位**。

## 7. Gate 3 最终判定

### 通过的内容

- 正交量级/谱网格和共同随机数设计有效；
- Rank-Only 被明确否决；
- Oracle/Residual uncertainty 能部分识别精确二次模型中的统计风险；
- n=6 residual uncertainty 正确 Abstain；
- 区域尺度和采样测度引发的结构偏差已被单独识别；
- 模型失配反例证明普通不确定性门控不足以提供迁移安全性。

### 未通过的内容

- 没有一种统一 operational gate 在所有噪声层和采样器上兼顾高覆盖与≤10%风险；
- 当前属性不能获得通用早期迁移资格；
- 普通残差不确定性不能检测中心 Hessian 与区域投影之间的目标错位；
- 仍未研究 Operational \(\widehat B,\widehat s\)。

## 8. 下一步决策

在进入迁移前，需要先选择研究对象：

1. 若坚持迁移中心本征曲率，必须加入局部尺度收缩/多尺度一致性检验，检测投影偏差；
2. 若接受迁移区域投影，则知识对象必须显式携带区域尺度和采样测度，且不能宣传为本征 Hessian；
3. Operational \(\widehat B,\widehat s\) 只能在上述语义确定后研究；
4. 在完成多尺度模型失配检测前，不进入 GP、BO、MAB 或源—目标迁移。

## 9. 核心图

- `results_step3/figures/figure1_noise_sample_risk.png`：样本数×噪声×采样器的 Rank-Only 错误风险；
- `results_step3/figures/figure2_holdout_risk_coverage.png`：保留集门控风险—覆盖率；
- `results_step3/figures/figure3_scale_misspecification_bias.png`：区域尺度×非二次性→投影偏差；
- `results_step3/figures/figure4_combined_stress.png`：组合压力下门控覆盖率与风险。
