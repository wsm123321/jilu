# 昂贵黑箱优化中局部知识的可迁移性研究计划

> **课题定位：** 先研究“什么值得迁移”，再研究“怎样迁移”。在阶段 5 完成前，不引入 MAB、全局导航、源区域提名或在线知识压缩。

## 1. 课题基本信息

### 中文题目

**昂贵黑箱优化中局部知识的可迁移性研究：变换规律、少样本可辨识性与净效用边界**

### 暂定英文题目

**What Is Transferable in a Local Landscape? Transformation Laws, Few-Shot Identifiability, and Utility Boundaries for Expensive Optimization**

### 周期与目标

- 周期：24 周；
- 目标：形成一篇机制研究型论文；
- 第一阶段解释局部性质为什么、何时以及以何种成本可以迁移。

### 核心研究问题

1. 局部区域中究竟包含哪些信息？
2. 哪些信息是目标函数的局部本征性质，哪些只是优化器产生的搜索状态？
3. 哪些性质经过平移、缩放、旋转或输出变换后仍然可比较？
4. 这些性质需要多少目标样本才能辨识？
5. 在何种相似程度和验证成本下，迁移能够真正节省昂贵评估？

### 预期成果

1. 一张局部性质分类与变换规律表；
2. 一张局部性质少样本可辨识性与迁移效用图谱；
3. 一个只迁移已验证属性的最小属性级迁移算法；
4. 一篇以机制、边界和负迁移风险为中心的论文初稿。

---

## 2. 研究范式

### 2.1 性质优先

借鉴不变算子研究的组织方式：

\[
\text{定义局部性质}
\rightarrow
\text{推导变换规律}
\rightarrow
\text{验证少样本可估计性}
\rightarrow
\text{再决定如何迁移}.
\]

必须区分“描述符具有不变性”和“使用该描述符的完整算法具有不变性”。后续实验既测试属性，也测试最终算法行为。

### 2.2 先研究适用边界

借鉴 MFBO 对“信息性 × 成本 × 收益”的研究方式，不默认辅助信息必然有用。局部迁移的真实成本包括：

- 用于验证源性质的目标评估；
- 错误接受源知识后浪费的目标预算；
- 源局部组件占用预算造成的机会成本；
- 模型训练与检索开销。

核心关系为：

\[
\boxed{
\text{局部信息性}
\times
\text{目标侧验证成本}
\longrightarrow
\text{净评估节省}
}
\]

### 2.3 拆解混合的局部经验

GL-LERC 式局部经验同时混合了位置、尺度、方向、排序、轨迹和局部最优解。课题不把它们作为整体迁移，而要分别判断每一项的本征性、可辨识性和净效用。

---

## 3. 三层研究对象

研究中必须严格区分：

| 层次 | 研究对象 | 示例 |
|---|---|---|
| 真实局部性质 | 目标函数在局部区域客观存在的性质 | 曲率、平滑性、各向异性、排序拓扑 |
| 性质估计器 | 根据有限样本估计真实性质的方法 | GP 长度尺度、局部二次模型、排序编码 |
| 迁移作用方式 | 估计结果进入目标优化器的方式 | 超参数初始化、软先验、输入预条件、源选择 |

示例：

\[
\text{局部平滑程度}
\rightarrow
\text{GP 长度尺度估计}
\rightarrow
\text{目标 GP 软先验}.
\]

任何实验都要避免把“真实景观变化”“采样位置变化”和“估计器训练不稳定”混为一谈。

---

## 4. 局部信息分解

使用规范化局部表示：

\[
x=a+Bu,\qquad u\in[-1,1]^d,
\]

\[
y=m+s\,g(u)+\epsilon.
\]

其中：

- \(a\)：局部区域中心；
- \(B\)：局部区域尺度、形状和方向；
- \(u\)：规范化局部坐标；
- \(g(u)\)：去除绝对位置和输出尺度后的局部景观；
- \(m,s\)：局部输出平移和尺度。

| 属性 | 本质 | 初步判断 | 可能用途 |
|---|---|---|---|
| 区域中心 \(a\) | 在哪里 | 等变信息，不是位置无关结构 | 后续区域提名 |
| 区域形状 \(B\) | 搜索多大、朝哪个方向 | 景观信息与优化器状态可能混合 | 信任域初始化，需先鉴别 |
| 平滑/曲率强度 | 函数变化多快 | 候选本征性质 | GP 尺度软先验 |
| 各向异性谱 | 不同方向的变化差异 | 谱可能稳健，方向向量风险高 | 核度量或输入预条件 |
| 局部排序拓扑 | 哪些相对位置更好 | 对正单调输出变换稳定 | 源选择或门控 |
| 搜索轨迹 | 优化器接下来如何移动 | 高度依赖优化器 | 后期策略迁移研究 |

### 第一阶段只研究

\[
\boxed{
\text{局部平滑强度}
+
\text{各向异性谱}
+
\text{局部排序拓扑}
}
\]

### 第一阶段暂不研究

- 源绝对位置；
- 完整旋转矩阵；
- Hessian 特征向量；
- 搜索轨迹；
- 高自由度仿射映射；
- 完整源响应模板。

区域中心仅作为“区域提名信息”，留到后续独立的 2×2 实验。

---

## 5. 正式研究假设

### H1：变换规律

规范化后，部分局部性质在平移、尺度和旋转变换下保持不变，或按照已知方式等变。

### H2：本征性

真正的局部景观性质在不同采样器和优化器下应基本稳定。如果描述符主要随优化器改变，它更接近搜索状态，而非通用景观知识。

### H3：少样本可辨识性

只有能在少量目标样本下可靠估计的性质，才适合用于昂贵优化的早期迁移。

### H4：作用机制

正确的源局部性质应先改善目标模型的预测或采集决策，进而改善在线优化结果。

### H5：效用边界

局部迁移收益由信息性和验证成本共同决定，不存在对所有源—目标关系都有效的局部知识。

---

## 6. 24 周总体计划

| 阶段 | 时间 | 核心问题 | 主要交付物 |
|---|---:|---|---|
| 0. 方法复现与代码审计 | 第 1—2 周 | 是否真正掌握三类研究方法？现有 ALAM 记录了什么？ | 3 个复现实验、局部信息清单 |
| 1. 局部性质形式化 | 第 3—5 周 | 每种属性在变换下如何变化？ | 性质规范、变换定律、单元测试 |
| 2. 可控局部景观生成器 | 第 6—9 周 | 能否只改变一个局部性质，其余保持不变？ | 生成器 v1、ground truth 属性 |
| 3. 本征性与少样本可辨识性 | 第 10—13 周 | 哪些性质不是优化器伪影？多少样本能估计？ | 稳定性矩阵、样本复杂度曲线 |
| 4. 固定区域下的迁移机制 | 第 14—17 周 | 属性通过预测、决策还是其他机制产生收益？ | 预测—决策—优化证据链 |
| 5. 信息性—成本—效用图谱 | 第 18—20 周 | 什么条件下迁移值得做？ | 效用热力图、软使用准则 |
| 6. 最小属性级迁移算法 | 第 21—24 周 | 属性级选择是否优于整个胶囊接受或拒绝？ | 最小算法、消融实验、论文初稿 |

---

## 7. 阶段 0：方法复现与系统审计（第 1—2 周）

### 任务 0.1：不变性配对实验

使用二维函数：

\[
f(x_1,x_2)=x_1^4+x_2^4.
\]

构造原始、平移、缩放和旋转版本。使用相同初始点与随机种子，对比一种近似不变方法和一种不具备对应不变性的方法。

**通过条件：** 相同景观经过无关坐标变换后，不变方法的搜索轨迹经逆变换应近似一致。

**交付物：**

- `reproduce_invariance.ipynb`；
- 4 张成对轨迹图；
- 1 页结论，区分坐标表示差异与真实景观变化。

### 任务 0.2：MFBO 小型效用热力图

使用 Branin，设置 5 个信息性等级、5 个成本等级和 10 个随机种子，绘制：

\[
\text{信息性}\times\text{成本}\rightarrow\Delta.
\]

重点掌握连续扫描适用边界、同时构造有利和不利场景，而不是只汇报手选正例。

### 任务 0.3：审计当前 ALAM-BO 局部信息

逐项列出 Scout 或局部区域保存的中心、半径、局部观测、GP 超参数、历史改进、源组件编号、区域状态、候选来源和 MAB 奖励，并标记为：

1. 目标函数局部性质；
2. 性质估计量；
3. 优化器搜索状态；
4. 调度统计量；
5. 尚不明确。

**阶段验收：** 能具体说明当前算法中“局部结构”对应哪些变量，以及哪些变量可能只是优化器产生的状态。

> 注：审计只用于建立外部对照清单。本课题的实现与实验必须位于本独立目录中，不复用或改写现有项目内容；如需引用现有项目代码，必须另行明确批准。

---

## 8. 阶段 1：局部性质的数学规范（第 3—5 周）

### 任务 1.1：定义变换族

输入变换：

\[
x'=Ax+b,\qquad A=Q\operatorname{diag}(s_1,\ldots,s_d),
\]

包括平移、逐维缩放和正交旋转。输出变换：

\[
y'=cy+d,\qquad c>0.
\]

限制 \(c>0\) 是为了保持最小化问题的优劣顺序。

### 任务 1.2：推导属性变换规律

区域中心与基矩阵：

\[
a'=Aa+b,\qquad B'=AB.
\]

局部 Hessian：

\[
H_{x'}=cA^{-\top}H_xA^{-1}.
\]

规范化坐标中的曲率矩阵：

\[
K=B^\top H_xB,
\qquad
K'=cK.
\]

进一步定义：

\[
\widetilde K=\frac{K}{\|K\|_F},
\]

则在正输出缩放下：

\[
\widetilde K'=\widetilde K.
\]

在固定规范化探针集 \(U=\{u_1,\ldots,u_m\}\) 上定义排序拓扑：

\[
R_g=\operatorname{rank}[g(u_1),\ldots,g(u_m)],
\]

其对严格单调递增输出变换保持不变。

### 任务 1.3：建立属性字典

每个属性必须记录：真实定义、估计器、变换规律、本征性判断和算法使用方式。

**阶段验收：** 未同时写清上述五项的属性不得进入后续实验；不再使用没有明确数学对象的“局部结构”“地形尺度”等表述。

---

## 9. 阶段 2：可控局部景观生成器（第 6—9 周）

### 9.1 二次局部景观

\[
g(u)=\frac12u^\top\Lambda u,
\qquad
\Lambda=\operatorname{diag}(\lambda_1,\ldots,\lambda_d).
\]

目标函数为：

\[
f(x)=c\,g(A^{-1}(x-b))+d+\epsilon.
\]

独立控制曲率强度、条件数、各向异性谱、旋转方向、区域位置和输入尺度。维度顺序：二维调试与可视化、五维主实验、十维后期扩展。

### 9.2 GP 采样局部景观

\[
g\sim\mathcal{GP}(0,k_{\ell,M}).
\]

使用已知真实核参数，研究真实长度尺度、各向异性强度、估计误差，以及源长度尺度是否能帮助目标 GP。

### 9.3 非凸和多模态局部景观

\[
g(u)=\frac12u^\top\Lambda u
+\beta\sum_j u_j^4
+\gamma\sum_j[1-\cos(\omega_j u_j)].
\]

分别控制非二次性、多模态强度和局部振荡频率。只有前两层稳定后才进入本层。

### 9.4 六类源—目标关系

| 场景 | 保持不变 | 改变内容 | 回答的问题 |
|---|---|---|---|
| A | 结构 | 位置 | 不迁移位置时，结构能否复用？ |
| B | 位置 | 结构 | 位置正确但结构错误是否有害？ |
| C | 曲率谱 | 旋转方向 | 只迁移谱能否抵抗旋转？ |
| D | 几何尺度 | 排序拓扑 | 仅看区域形状是否会误判？ |
| E | 平滑强度 | 多模态程度 | 单一长度尺度是否信息不足？ |
| F | 无 | 全部异构 | 完全错误知识的代价是什么？ |

**阶段验收与自动测试：**

- 只平移时，ground truth 结构属性不变；
- 只旋转时，曲率特征值谱不变；
- 只改变曲率时，位置和方向不变；
- 只改变排序拓扑时，区域几何不变。

---

## 10. 阶段 3：本征性与少样本可辨识性（第 10—13 周）

### 10.1 两轴正交实验

**轴一：同一景观，不同采样机制。** 使用 Sobol/LHS、均匀随机、Target-Only 局部 BO、TuRBO 式局部轨迹、CMA-ES 或局部 EA 种群，比较区域半径、协方差、GP 长度尺度、各向异性强度和排序编码。

**轴二：同一规范景观，不同坐标变换。** 固定采样机制，施加平移、缩放、旋转和输出缩放，检查规范化描述符的稳定性。

### 10.2 样本量

\[
n_t\in\{3,5,8,12,20\},
\qquad
n_s\in\{50,100\}.
\]

明确每个属性在 3、5、8、12 和 20 个目标点下的可辨识程度。需要 20 点才能稳定识别的属性不得用于早期 few-shot 迁移。

### 10.3 指标

不变属性的变换残差：

\[
E_{\mathrm{inv}}
=
\frac{\|\widehat\phi(T\mathcal L)-\widehat\phi(\mathcal L)\|}
{\|\widehat\phi(\mathcal L)\|+\epsilon}.
\]

等变属性的变换残差：

\[
E_{\mathrm{eq}}
=
\frac{\|\widehat\psi(T\mathcal L)-\rho_T(\widehat\psi(\mathcal L))\|}
{\|\rho_T(\widehat\psi(\mathcal L))\|+\epsilon}.
\]

估计误差：

\[
E_{\mathrm{est}}
=
\frac{\|\widehat\phi-\phi^\star\|}
{\|\phi^\star\|+\epsilon}.
\]

源检索指标：Recall@1、Recall@3、正确源平均排名、错误源误选率。

### 10.4 Pilot 暂定筛选线

- 无噪声纯变换下，中位变换残差小于 0.1；
- \(n_t\leq 8\) 时，Recall@3 至少为 0.7；
- 同结构跨优化器方差明显小于不同结构间方差；
- bootstrap 区间不能严重失准。

阈值经 pilot 校准后冻结并预注册。未达标属性应归类为仅离线可用、仅中后期可用、搜索状态或暂不研究，而不是简单宣布“无用”。

---

## 11. 阶段 4：固定区域下的迁移机制（第 14—17 周）

### 11.1 固定条件

所有方法共享同一目标局部区域、初始目标样本、GP 模型族、采集函数、候选集、真实评估预算和成对随机种子，只改变是否使用某项源属性。

### 11.2 必要对照

| 方法 | 使用的信息 | 用途 |
|---|---|---|
| Target-Only | 不使用源性质 | 基线 |
| Correct-Source | 正确源性质 | 正迁移上限 |
| Wrong-Source | 明确失配性质 | 负迁移压力测试 |
| Random-Source | 随机源性质 | 排除偶然初始化收益 |
| Oracle-Target | 真实目标性质 | 理想上界 |

每个属性分别采用初始化、软先验和固定参数三种注入方式，以判断收益来自一次 warm start 还是持续结构约束。

### 11.3 三层证据

**预测机制：** prequential NLL、标准化 RMSE、区间覆盖率、过度自信率。必须在获得新观测前计算预测分布，禁止用训练误差证明源模型更好。

**决策机制：** 采集函数排序、Top-K 候选重合率、候选点真实局部 regret、候选点聚集程度，以及：

\[
\rho_\alpha
=
\operatorname{Spearman}(\alpha_{\mathrm{source}},\alpha_{\mathrm{oracle}}).
\]

**优化机制：** 归一化 Best-So-Far AUC、达到目标 regret 所需评估数、最终 simple regret、负迁移 seed 比例和最差 10% seed 的尾部损失。

### 11.4 属性准入规则

属性必须形成：

\[
\text{属性正确}
\rightarrow
\text{预测更好或决策更好}
\rightarrow
\text{在线优化更好}
\]

的可解释证据链，才进入阶段 5。参数更接近但预测和优化不改善的属性不迁移；只改善预测或决策的属性需明确限定宣传口径并调整作用方式。

---

## 12. 阶段 5：局部迁移效用相图（第 18—20 周）

### 12.1 连续信息性

真实属性相似性可定义为：

\[
I_\phi^\star
=
\exp\left(-\frac{d^2(\phi_s^\star,\phi_t^\star)}{\tau_\phi^2}\right),
\]

或使用固定规范化探针集上的真实秩相关：

\[
I_{\mathrm{rank}}^\star
=
\frac{1+\rho_S(g_s(U),g_t(U))}{2}.
\]

\(I^\star\) 只用于 ground-truth 机理分析；在线算法只能使用少量目标数据得到的 \(\widehat I_n\)。

### 12.2 验证成本

\[
b_{\mathrm{probe}}\in\{0,2,4,6,8,10\},
\qquad
\rho_{\mathrm{val}}=\frac{b_{\mathrm{probe}}}{B_{\mathrm{total}}}.
\]

同时记录错误接受前浪费的目标评估 \(b_{\mathrm{misalloc}}\)。

### 12.3 净评估节省

\[
\Delta_\phi(r^\star)
=
\frac{B_0(r^\star)-B_\phi(r^\star)}{B_0(r^\star)}.
\]

其中 \(B_\phi\) 包括所有验证点和其他真实目标评估。\(\Delta_\phi>0\) 表示迁移有净收益，\(\Delta_\phi<0\) 表示迁移浪费预算。

### 12.4 核心图

每种属性分别绘制：

- 横轴：真实局部信息性 \(I_\phi^\star\)；
- 纵轴：验证成本比例 \(\rho_{\mathrm{val}}\)；
- 颜色：预算折扣 \(\Delta_\phi\)；
- 分层：目标样本数、噪声和变换类型。

同时绘制：

\[
P(\Delta_\phi>0\mid\widehat I_n,\rho_{\mathrm{val}})
\]

的校准图，形成经验性的软使用准则，而不是宣称普适硬阈值。

---

## 13. 阶段 6：最小属性级迁移算法（第 21—24 周）

### 13.1 最小实验场景

- 一个固定目标局部区域；
- 一个 Target-Only 模型；
- 一个或两个 Source-Informed 模型；
- 每种属性拥有独立证据；
- 不涉及区域竞争和源位置提名。

### 13.2 属性级证据更新

对属性 \(\phi_j\) 维护：

\[
S_{j,t}
=
\lambda S_{j,t-1}
+
\log p_j(y_t\mid x_t,\mathcal D_{t-1})
-
\log p_0(y_t\mid x_t,\mathcal D_{t-1}),
\]

并令：

\[
w_{j,t}=\sigma(\eta S_{j,t}).
\]

允许不同属性分别被接受、部分接受或拒绝，而不是整体接受一个源胶囊。

### 13.3 对照方法

1. Target-Only；
2. 整个源胶囊硬接受；
3. 整个源胶囊硬拒绝；
4. 单一最好属性；
5. 属性级软门控；
6. Oracle 属性选择。

### 13.4 重点场景

- 位置错误、平滑性正确；
- 平滑性正确、方向错误；
- 曲率谱正确、排序拓扑错误；
- 区域尺度正确、多模态结构错误。

### 13.5 通过标准

属性级方法应：

- 在部分匹配场景中优于整体拒绝；
- 在部分失配场景中优于整体接受；
- 在全部失配场景中接近 Target-Only；
- 其收益可由阶段 4 的预测或决策证据解释。

---

## 14. 实验纪律

1. **严格等目标评估预算：** 试探点、门控验证点、迁移候选和 Target-Only 候选均计入相同目标预算。
2. **成对随机种子：** 方法间共享目标实例、初始样本、噪声实现和候选池。
3. **Oracle 与在线指标分离：** Oracle 相似性仅用于机理图，不能作为算法输入。
4. **开发集与保留测试集分离：** 二次和 GP 采样函数用于开发；Ackley、Rastrigin、Branin 等用于保留测试；现有 ALAM 问题只用于最终外部验证。阈值不得在保留测试集上重调。
5. **统计分析：** 最终实验采用 30 seeds，报告配对差值、bootstrap 置信区间、效应量、负迁移比例、尾部风险和多重比较校正。
6. **单假设消融：** 每轮实验只改变一个关键因素，避免不可归因的系统级堆叠。
7. **诚实报告边界：** 同时展示有利、不利和无效场景，不以平均胜率掩盖负迁移。

---

## 15. 下次组会前的两周任务

### 第 1—3 天：方法梳理

提交不超过 3 页的说明：

1. 不变算子研究链：性质定义 → 理论推导 → 配对实验 → 算法设计；
2. MFBO 研究链：信息性 × 成本 → 净效用 → 使用准则；
3. GL-LERC 局部经验中混合的位置、尺度、方向、排序、轨迹和局部最优解。

### 第 4—7 天：局部景观生成器 v0

实现：

```text
LocalPatch
├── canonical_function(u)
├── transform_input(A, b)
├── transform_output(c, d)
├── sample_source(n)
├── sample_target(n)
└── ground_truth_descriptor()
```

第一版只做二维二次函数，并支持平移、统一缩放、各向异性缩放、旋转以及输出平移与正缩放。

### 第 8—10 天：三个描述符

实现：

1. 规范化曲率强度；
2. 规范化曲率特征值谱；
3. 固定 Sobol 探针集上的局部排序编码。

编写不变性单元测试：平移不变、规范化缩放不变、旋转后谱不变、输出正缩放后排序不变。

### 第 11—14 天：第一轮 Pilot

设置：

- \(d=2\)；
- 3 类二次景观；
- 10 个随机变换；
- \(n_t\in\{3,5,8,12\}\)；
- 10 seeds；
- 暂不运行完整在线优化。

必须提交：

1. 变换残差图 \(E_{\mathrm{inv}}\)；
2. 估计误差—目标样本数曲线 \(E_{\mathrm{est}}(n_t)\)；
3. 同一景观在 Sobol、随机和简单局部 BO 轨迹下的描述符箱线图；
4. 正确源与错误源对目标模型 prequential NLL 的影响图。

### 组会准入问题

进入 MAB、导航和完整在线优化前，必须回答：

1. 哪些描述符在理论和实验上具有变换稳定性？
2. 哪些描述符主要受采样器影响，不能直接称为景观性质？
3. 哪些描述符在 8 个目标点以内能被较稳定估计？
4. 正确源性质是否带来目标预测增益，错误源是否造成过度自信？

---

## 16. 失败处理与阶段决策

| 观察结果 | 决策 |
|---|---|
| 平滑性稳定、少样本可识别且节省预算 | 保留为局部核尺度软先验 |
| 各向异性强度有效但方向不稳定 | 只迁移谱或强度，不迁移方向 |
| 排序拓扑能筛源但不改善模型 | 只用于门控，不作为模型参数 |
| 区域半径高度依赖优化器 | 归类为搜索状态，不进入长期知识库 |
| 搜索轨迹只对特定优化器有效 | 另立策略迁移课题 |
| Target-Only 局部化解释全部收益 | 降低“迁移”在论文中的中心地位 |
| 只有区域提名有效 | 转向跨盆发现和目标侧准入研究 |
| 只有结构有效 | 取消位置迁移，集中研究结构先验 |
| 部分属性匹配时属性级方法有效 | 作为后续 ALAM-BO v2 的候选核心机制 |

具体失败处理：

- 变换测试失败：检查数学定义、规范化步骤和数值误差；在修复前停止迁移实验。
- 跨采样器不稳定：将描述符降级为搜索状态，或设计更稳健的估计器后重新进入阶段 3。
- 少样本不可辨识：转为中后期属性，不用于早期门控。
- 正确源不改善预测或决策：停止该属性的迁移开发，避免仅凭参数相似性继续扩展。
- 错误源导致显著过度自信：禁止固定参数注入，改用可被目标数据覆盖的软先验。
- 净折扣不为正：即使离线指标良好，也不进入最小迁移算法。

---

## 17. 知识积累与压缩的时机

顺序必须是：

\[
\text{识别有效性质}
\rightarrow
\text{验证少样本可辨识性}
\rightarrow
\text{验证净优化效用}
\rightarrow
\text{再决定存储什么}.
\]

只有通过验证的规范化属性及其估计不确定性才进入知识库。若区域半径和轨迹主要反映优化器状态，则只保存在任务情景日志中，不压缩为通用景观知识。

---

## 18. 论文主线

现有方法往往整体迁移局部位置、尺度、方向、响应排序和搜索轨迹，因此难以解释收益来源。本研究首先区分目标函数局部本征性质与优化器搜索状态，建立属性在平移、缩放、旋转及输出变换下的规律；随后研究其少样本可辨识性，排除需要大量数据或高度依赖优化器的属性；进一步从局部信息性和目标侧验证成本两个维度研究净评估收益；最后设计属性级条件迁移，使目标任务只接受获得在线证据支持的平滑性、各向异性或排序属性。

> **核心结论目标：局部区域并不天然构成可迁移知识。只有与无关坐标表达和具体优化器状态解耦、能够由少量目标观测识别，并且带来的评估节省超过验证与误用成本的局部属性，才应进入迁移优化器。**
