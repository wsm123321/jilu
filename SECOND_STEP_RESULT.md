# 第二步结果：二维无噪声少样本可辨识性 Pilot

## 1. 结论摘要

第二步的 Oracle 表示层 Pilot 已完成。实验覆盖 9 个二维正定二次景观、3 类采样设计、6 个样本量和 50 个成对随机种子，共 8100 条 run。

当前最准确的结论是：

> 在真实局部图表 \(B\)、输出尺度 \(s\) 已知，函数严格为无噪声完整二次模型，并且设计矩阵满秩时，二维规范曲率可以从 6 个一般位置样本开始恢复到双精度数值误差；但刚好 6 点时的设计条件数具有明显长尾，轨迹型聚集采样最严重。因此本轮只证明“Oracle 层无噪声代数可恢复性”，尚未证明带噪声、表示未知或实际优化轨迹下的 operational 可辨识性。

本结果不证明属性有迁移价值，也不构成进入 GP、BO、MAB 或源选择的依据。

## 2. 定义修正

研究计划已统一使用：

\[
K=\frac1sB^\top H_xB.
\]

\(\|K\|_F/\sqrt2\) 统一称为“相对于规定局部图表的曲率量级”，不再与数学光滑性或 GP 长度尺度混称。

同时明确：

- **Oracle 表示层：** 真实 \(B,s,m\) 已知，本轮研究对象；
- **Operational 表示层：** 使用实际规则得到 \(\widehat B,\widehat s\)，尚未研究。

完整二次模型只用于二维机制 Pilot。五维完整二次模型需要 21 个系数，不能在最多 20 点的 few-shot 设置中唯一辨识；本轮结果不得外推到五维。

## 3. 实验身份

- 景观谱比：\(r\in\{1,4,16\}\)；
- 规范方向：\(\theta\in\{0,\pi/8,\pi/4\}\)；
- 采样器：Sobol prefix、均匀随机、轨迹型偏置采样；
- 样本量：\(n\in\{3,5,6,8,12,20\}\)；
- 每条件：50 seeds；
- 总 run 数：8100；
- 噪声：0；
- 拟合器：SVD 完整二次最小二乘；
- SPD 投影：未使用；
- 原始 CSV SHA-256：`0a408cb37b675975cb8eb01f61cd1a4cff54c4185fbbe805a760a49872be0b23`。

Sobol 非二次幂样本量使用长度为 \(2^{\lceil\log_2n\rceil}\) 的 scrambled net 前缀，不声称这些前缀保持完整 net 的标准 balance property。

## 4. Abstain 与可估计率

| 采样设计 | n=3 | n=5 | n=6 | n=8 | n=12 | n=20 |
|---|---:|---:|---:|---:|---:|---:|
| Sobol prefix | 0% | 0% | 100% | 100% | 100% | 100% |
| Uniform | 0% | 0% | 100% | 100% | 100% | 100% |
| Trajectory | 0% | 0% | 96.89% | 99.78% | 100% | 100% |

其中百分比为 `not Abstain` 比例。\(n=3,5\) 全部以 `insufficient_samples` 拒绝，满足冻结规则。轨迹型采样在 \(n=6,8\) 出现少量严格秩亏设计，说明点数达到参数数并不自动保证可辨识。

## 5. 满秩后的估计误差

所有满秩条件下：

- 归一化谱误差中位数约为 \(10^{-15}\)；
- 谱误差 90% 分位数均不超过约 \(1.64\times10^{-14}\)；
- Hessian 相对误差 90% 分位数均不超过约 \(2.35\times10^{-14}\)；
- 固定探针成对次序准确率中位数为 1.0。

这是因为数据无噪声、真实函数严格属于拟合模型族。该结果主要验证实现、系数约定和代数可恢复性，不能作为现实 few-shot 稳定性的充分证据。

## 6. 设计条件数是当前主要风险

### n=6 的设计矩阵条件数

| 采样设计 | 中位数 | 90% 分位数 |
|---|---:|---:|
| Sobol prefix | 31.55 | 255.48 |
| Uniform | 85.62 | 664.64 |
| Trajectory | 176.87 | 1655.61 |

### n=8 的设计矩阵条件数

| 采样设计 | 中位数 | 90% 分位数 |
|---|---:|---:|
| Sobol prefix | 12.14 | 27.26 |
| Uniform | 27.10 | 75.87 |
| Trajectory | 66.31 | 242.61 |

随着样本量增加，Sobol prefix 和均匀随机的条件数明显下降。轨迹型采样即使在 \(n=20\) 时条件数 90% 分位数仍约为 102.17，反映其局部聚集和相关性。

无噪声下高条件数尚未造成明显误差，但它预示微小观测噪声、舍入误差或非二次模型失配可能被显著放大。因此不能用本轮接近零的拟合误差宣布轨迹采样下属性稳定。

## 7. 四张核心图

1. `results/figure1_identifiable_rate.png`：可估计率—样本数；
2. `results/figure2_spectrum_error.png`：谱误差中位数和 90% 分位数—样本数；
3. `results/figure3_error_vs_condition.png`：Hessian 误差—设计条件数；
4. `results/figure4_sampler_variance.png`：估计主谱占比的同景观跨采样器方差与不同谱间方差。

第四张图使用估计出的主谱占比，而不是误差方差，统计对象直接对应“采样器变化是否淹没景观差异”。在当前无噪声满秩设置下，跨采样器差异接近数值误差，不同真实谱之间差异占主导；这仍只成立于 Oracle 表示和模型完全正确的条件下。

## 8. 验收判断

| 冻结条件 | 结果 |
|---|---|
| n<6 全部 Abstain | 通过 |
| 无噪声、满秩时接近数值精度 | 通过 |
| n≤8 满秩率至少 80% | 通过；最差为 trajectory n=6 的 96.89% |
| 谱中位误差 <0.1，P90 <0.25 | 通过，但属于无噪声模型正确条件 |
| Sobol prefix 与均匀随机定性一致 | 通过 |
| 报告采样器风险而非只报告成功子集 | 通过 |

### 属性分类

本轮最多支持：

- 曲率量级：`Oracle-layer algebraically identifiable`；
- 各向异性谱：`Oracle-layer algebraically identifiable`；
- 模型预测的固定探针排序签名：`Oracle-layer algebraically identifiable`。

三者均**尚不能**标记为 `Early-transfer candidate`，原因是尚未通过噪声、模型失配、Operational \(\widehat B,\widehat s\) 和真实轨迹压力测试。

## 9. 独立验证改进

新增了两条非循环验证：

1. `recover_canonical_curvature(H_x, B, s)` 接收外部构造的物理 Hessian；
2. 从函数值用中心有限差分得到数值 Hessian，再恢复规范曲率。

测试还覆盖随机 SPD 曲率、随机方向、条件数最高约 \(10^3\) 和随机可逆图表。全套测试目前为 124 项。

## 10. 下一道研究关口

下一步仍不进入迁移，应优先检验数值条件风险是否转化为实际估计失败：

1. 保持二维与 Oracle \(B,s\) 不变；
2. 加入相对噪声 \(\sigma/s\in\{0.01,0.05,0.10\}\)；
3. 加入轻微非二次项，分离观测噪声和模型失配；
4. 在开发集上研究条件数与误差/覆盖的关系，再冻结 `ill_conditioned` Abstain 阈值；
5. 随后单独开展 Operational \(\widehat B,\widehat s\) 实验。

如果在微小噪声下 \(n\le8\) 的误差或尾部风险明显失控，则这些属性应降级为 `Mid-stage candidate` 或 `Not operationally identifiable`，而不是继续包装进迁移算法。
