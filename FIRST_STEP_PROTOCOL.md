# 第一步冻结协议：先证明研究对象定义正确

## 1. 本步只回答一个问题

> 在完全已知的二维二次局部景观中，能否定义出不依赖物理坐标表达、并具有明确输出变换规律的局部属性？

本步不研究论文复现、不拟合 GP、不运行 BO、不判断迁移收益，也不使用现有 ALAM-BO 代码。

## 2. 最小研究对象

规范局部坐标为

\[
u\in[-1,1]^2,
\]

规范景观为

\[
g(u)=\frac12u^\top\Lambda u,
\qquad
\Lambda=\operatorname{diag}(\lambda_1,\lambda_2),
\qquad \lambda_i>0.
\]

物理坐标及输出定义为

\[
x=a+Bu,
\qquad
y=m+s\,g(u),
\qquad s>0,
\]

其中 \(a\in\mathbb R^2\)，\(B\in\mathbb R^{2\times2}\) 可逆。第一步使用无噪声函数，避免把定义问题和统计估计问题混合。

## 3. 变换约定

对已有物理问题施加

\[
x'=Ax+b,
\qquad y'=cy+d,
\]

其中 \(A\) 可逆、\(c>0\)。变换后同一规范景观的参数应为

\[
a'=Aa+b,
\qquad B'=AB,
\qquad m'=cm+d,
\qquad s'=cs.
\]

规范曲率 \(\Lambda\) 不变。这里研究的是同一局部景观的坐标重表达，不是另造一个具有不同曲率的任务。

## 4. Ground-truth 属性

### 4.1 物理 Hessian

\[
H_x=sB^{-\top}\Lambda B^{-1}.
\]

它是等变量而非不变量。变换后必须满足

\[
H_{x'}=cA^{-\top}H_xA^{-1}.
\]

### 4.2 去除输入表达和输出尺度后的规范曲率

\[
K=\frac{1}{s}B^\top H_xB=\Lambda.
\]

在本协议的输入仿射变换和正输出仿射变换下，\(K\) 应保持不变。

### 4.3 曲率强度

\[
q=\frac{\|K\|_F}{\sqrt{2}}.
\]

它表示规范局部坐标中每维的整体弯曲强度。它依赖局部坐标域的定义，因此只能在共同规范域 \([-1,1]^2\) 下比较。

### 4.4 归一化曲率谱

令 \(\mu_i\) 为 \(K\) 的特征值，定义

\[
p_i=\frac{|\mu_i|}{\sum_j|\mu_j|},
\]

并按降序排列。该谱保留相对各向异性而去除整体强度。

同时记录条件数

\[
\kappa=\frac{\max_i|\mu_i|}{\min_i|\mu_i|}.
\]

第一步限定 \(\Lambda\) 正定，因此分母非零。

### 4.5 局部排序编码

冻结一组规范化探针 \(U=\{u_1,\ldots,u_M\}\)，计算 \(g(u_i)\) 的稳定排序。比较变换前后时必须使用同一组 \(u_i\)，其对应物理点分别为 \(a+Bu_i\) 与 \(a'+B'u_i\)。

在 \(c>0\) 的输出仿射变换下，排序必须完全一致。若出现相等值，则以探针原始编号稳定破同序；测试探针应尽量避免人为制造大量 ties。

## 5. 第一批场景

冻结三类二维曲率：

| 名称 | \(\Lambda\) | 用途 |
|---|---:|---|
| isotropic | diag(1, 1) | 各向同性基准 |
| mild | diag(1, 4) | 温和各向异性 |
| strong | diag(1, 16) | 强各向异性 |

每类场景测试：

1. 仅平移；
2. 统一输入缩放；
3. 各向异性输入缩放；
4. 旋转；
5. 旋转与各向异性缩放组合；
6. 正输出缩放和平移；
7. 输入、输出联合变换。

## 6. 自动测试与通过标准

采用双精度和确定性测试。对于连续数值量使用

\[
\operatorname{rtol}=10^{-10},\qquad
\operatorname{atol}=10^{-12}.
\]

必须全部通过：

1. `to_physical` 与 `to_canonical` 互为逆映射；
2. 变换前后的对应物理点具有满足 \(y'=cy+d\) 的函数值；
3. 解析物理 Hessian 满足等变公式；
4. 从 \(B,H_x,s\) 恢复的 \(K\) 等于 \(\Lambda\)；
5. 曲率强度、归一化谱和条件数在所有坐标重表达后不变；
6. 固定规范探针的排序编码在所有正输出仿射变换后完全相同；
7. 非正输出尺度 \(c\le0\) 被接口拒绝，防止无意改变最小化顺序；
8. 奇异输入变换或奇异区域基矩阵被接口拒绝。

## 7. 本步的停止规则

- 若解析规律测试失败，不进入任何有限样本估计实验；
- 若失败源于定义冲突，先修改本协议并记录版本，不通过调松阈值掩盖；
- 只有全部 ground-truth 测试通过后，下一步才引入采样器和估计误差；
- 后续估计器即使失败，也不能反推 ground-truth 属性不存在，只能说明该估计器或样本预算下不可辨识。

## 8. 下一步接口（本步不实现）

下一步会在保持生成器不变的前提下增加：

- `sample(n, design, seed)`；
- 局部二次回归估计器；
- 估计器的设计矩阵秩和条件数资格；
- \(n_t\in\{3,5,8,12,20\}\) 下的估计误差；
- Sobol、随机和轨迹型采样的正交对照；
- 明确的 `Abstain` 输出，而不是在不可辨识时返回伪精确属性。
