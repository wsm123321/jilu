# 第一步结果：Ground-truth 定义与变换规律验证

## 结论摘要

第一步已完成。二维正定二次局部景观的规范坐标表示、物理坐标重表达、输出正仿射变换以及 ground-truth 描述符已经实现，并通过自动测试。

本结果只支持：

> 在 Oracle 表示层，即局部区域坐标图表 \(B\) 和输出尺度 \(s\) 已知时，规范曲率、相对于规定图表的曲率量级、相对曲率谱以及固定规范探针上的排序编码，可以与物理坐标的平移、缩放、旋转及正输出仿射变换解耦。

本结果不支持：

- 这些属性能由少量样本可靠估计；
- GP 长度尺度等估计器等价于真实局部平滑性；
- 属性跨源—目标任务可迁移；
- 使用属性能够改善预测、采集决策或在线优化；
- 完整迁移算法具有不变性。

## 已实现对象

### `LocalPatch`

表示

\[
x=a+Bu,
\qquad
f(x)=m+s\frac12u^\top\Lambda u.
\]

支持：

- 规范坐标与物理坐标双向映射；
- 规范坐标和物理坐标下的函数求值；
- 解析物理 Hessian；
- 由物理 Hessian、区域图表和输出尺度恢复规范曲率；
- 输入仿射变换与正输出仿射变换下的同景观重表达；
- 对奇异矩阵、非正输出尺度和非法曲率的显式拒绝。

### Ground-truth 描述符

- 规范曲率矩阵；
- Frobenius 意义下、相对于规定局部图表的曲率量级；
- 归一化绝对特征值谱；
- 曲率条件数；
- 固定规范探针的稳定排序编码。

## 测试设计

曲率场景：

- `diag(1, 1)`；
- `diag(1, 4)`；
- `diag(1, 16)`。

变换场景：

- 平移；
- 统一输入缩放；
- 各向异性输入缩放；
- 旋转；
- 旋转、缩放和平移组合；
- 输出正缩放和平移；
- 输入与输出联合变换。

测试覆盖：

1. 坐标往返一致性；
2. 对应点的输出仿射规律；
3. 物理 Hessian 等变规律；
4. 规范曲率恢复；
5. 曲率描述符不变性；
6. 固定规范探针排序不变性；
7. 非法变换拒绝。

## 运行结果

```text
67 passed in 0.18s
```

运行环境：

- Python 3.12.13；
- NumPy 2.4.3；
- pytest 9.0.3。

## 当前最重要的认识

物理坐标中的 Hessian 本身不是不变量。只有把区域图表与输出尺度纳入定义，构造

\[
K=\frac{1}{s}B^\top H_xB,
\]

才能恢复共同规范坐标中的曲率。也就是说，后续比较局部属性时必须明确“比较发生在哪个局部坐标图表中”。如果 \(B\) 本身来自优化器信任域，而不是真实区域图表，那么由它得到的描述符可能仍含有优化器状态。这将是下一步必须单独检验的风险。

## 下一步建议

下一步进入“估计器层”，但仍不做迁移算法。建议顺序为：

1. 冻结采样设计和样本量；
2. 实现带截距、线性项和完整二次项的局部回归；
3. 在返回曲率估计前检查设计矩阵秩与条件数；
4. 对不可辨识情况输出 `Abstain`，禁止使用默认伪属性；
5. 分别比较 Sobol、均匀随机和轨迹型采样；
6. 报告“可估计率”和“条件于可估计的误差”，不能只报告成功拟合子集误差；
7. 先做无噪声，再逐级加入噪声。

二维完整二次模型含 6 个系数，因此：

- \(n<6\)：代数上不可唯一识别完整二次模型；
- \(n=6\)：虽可能恰好可解，但通常缺乏稳定性余量；
- \(n\ge8\)：可以开始研究设计质量与误差，但不能预设一定稳定。

因此下一轮样本量建议保留 \(\{3,5,8,12,20\}\)，其中 3 和 5 点不是为了期待成功，而是为了明确展示不可辨识边界。
